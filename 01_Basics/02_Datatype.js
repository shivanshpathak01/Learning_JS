"use strict" /* if we write this in any file then the file is used as newer version of javascript */
console.log(typeof null); // It says that null is an object
console.log(typeof undefined); // it is being given a special type undefined  